#include "MatrixMath.h"

MyMatrix LU_expansion(MyMatrix A)
{
	//diag is still the same
	//upper is U and lower is L
	
	return MyMatrix();
}

MyMatrix multiplication(MyMatrix a, MyMatrix b) //преопределить оператор
{
	MyMatrix c;

	return c;
}